# === 核心依赖（阶段一必装） ===
torch>=2.2.0
torchvision
diffusers>=0.27.0
transformers>=4.38.0
accelerate>=0.27.0
safetensors>=0.4.0
Pillow>=10.0.0
PyYAML>=6.0
tqdm>=4.65.0

# === 阶段三：UI 拆解（按需安装） ===
# segment-anything
# opencv-python
# numpy

# === 阶段四：后处理（按需安装） ===
# realesrgan
# basicsr

# === 阶段五：界面（按需安装） ===
# gradio>=4.0
